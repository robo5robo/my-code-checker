# CLAUDE.md

## هدف المشروع

منصة **مجانية** لفحص وتشغيل أكواد الطلاب. يكتب الطالب كوده في المتصفح، فيستطيع:
- **تشغيله** ورؤية المخرجات. التشغيل يتم داخل متصفح الطالب فقط، وليس على الخادم.
- **تحليله**: فحص ساكن للكود مع شرح للأخطاء وخطوات الإصلاح وفحص أمني وتقييم للجودة بالعربية عبر الذكاء الاصطناعي.

## التقنيات المستخدمة

| الطبقة | التقنية |
|---|---|
| لغة الخادم | Python |
| إطار الخادم | Flask + Flask-CORS |
| خادم الإنتاج | Gunicorn |
| الذكاء الاصطناعي | Google Gemini عبر مكتبة `google-generativeai`، والمفتاح في متغير البيئة `GEMINI_API_KEY` |
| الفحص الساكن | `pylint` لبايثون، و`gcc` و`g++` مع `-fsyntax-only` لـ C وC++ (فحص الصياغة فقط، دون ترجمة أو تشغيل) |
| الواجهة | HTML/CSS/JavaScript عادية في `index.html`، بدون إطار |
| محرر الكود | Monaco Editor (من CDN) |
| التشغيل في المتصفح | Pyodide لبايثون، وJavaScript داخل Web Worker معزول مع حد زمني |
| قاعدة البيانات | لا توجد |
| النشر | Render عبر `Dockerfile`، والخادم يستمع على المنفذ من متغير البيئة `PORT` |

### الملفات
- `app.py`: خادم Flask. المسار `GET /` يعرض الصفحة، والمسار `POST /api/check-code` يجري الفحص الساكن ثم التحليل بالذكاء الاصطناعي.
- `index.html`: الواجهة: محرر Monaco، واختيار اللغة، وزر التشغيل، وزر التحليل، ولوحة النتائج.
- `requirements.txt`: مكتبات بايثون.
- `Dockerfile`: صورة النشر على Render.

### التشغيل محلياً
```bash
pip install -r requirements.txt
export GEMINI_API_KEY=...   # اختياري، وبدونه تظهر مخرجات الفحص الساكن فقط
python app.py               # http://127.0.0.1:5000
```

## قواعد ثابتة (إلزامية)

1. **لا تضع أي مفاتيح أو أسرار داخل الكود.** استخدم متغيرات البيئة فقط، مثل `os.environ.get("GEMINI_API_KEY")`، ولا ترفع ملفات `.env` إلى المستودع.
2. **لا تشغّل كود الطالب مباشرة على الخادم أبداً.** الخادم يسمح فقط بالفحص الساكن الذي لا ينفّذ الكود (`pylint`، و`gcc -fsyntax-only`) وبإرساله إلى الذكاء الاصطناعي. أي تشغيل فعلي يتم داخل متصفح الطالب، في Web Worker أو Pyodide.
3. **اشرح كل تعديل مهم قبل تنفيذه**: ماذا سيتغيّر، ولماذا، وما الملفات المتأثرة.
